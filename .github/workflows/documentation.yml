name: 📚 Documentação

on:
  push:
    paths:
      - "**/*.md"
  pull_request:
    paths:
      - "**/*.md"

jobs:
  docs-check:
    name: Validação da Documentação
    runs-on: ubuntu-latest

    steps:
      - name: Clonar repositório
        uses: actions/checkout@v4

      - name: Verificar existência do README
        run: |
          if [ ! -f README.md ]; then
            echo "README.md não encontrado!"
            exit 1
          fi

      - name: Verificar arquivos Markdown vazios
        run: |
          while IFS= read -r -d '' file; do
            if [ ! -s "$file" ]; then
              echo "Arquivo vazio encontrado: $file"
              exit 1
            fi
          done < <(find . -name "*.md" -print0)

      - name: Confirmação final 🪙
        run: echo "Documentação validada com sucesso ✅"